// ---------------------------------------------------------------------------
// Types
// ---------------------------------------------------------------------------

/** Accepted input shapes for create() */
export type JSONInput = object | unknown[] | string;

/** Accepted input shapes for fromMJKCJSON() */
export type MJKCJSONInput = Blob | ArrayBuffer | Uint8Array | string;
